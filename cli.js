#!/usr/bin/env node
'use strict';

// TODO:
async function main() {
    const version = process.versions.node;

    console.log("Magic Framework ✨");

}

main()
    .then(() => process.exit(0))
    .catch(() => process.exit(1));